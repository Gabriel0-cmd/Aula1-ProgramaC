#include <stdio.h> //permite usar printf

int main(void)//main() é o ponto inicial do programa
{
    printf("Hello, World!\n");//se remover o /n o cursor fica na mesma linha, ou seja, não pula linha
    return 0;//o programa continua funcionando
}